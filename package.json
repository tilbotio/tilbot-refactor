{
  "name": "tilbot",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "workspaces": {
    "packages": [
      "frontend",
      "backend",
      "socket-io",
      "app",
      "tests/unit",
      "tests/integration/app",
      "tests/integration/frontend"
    ]
  },
  "scripts": {
    "dev:frontend": "yarn workspace frontend dev",
    "dev:backend": "yarn workspace backend start",
    "dev": "concurrently \"yarn dev:frontend\" \"yarn dev:backend\"",
    "prod": "cross-env TILBOT_ENV=web concurrently \"yarn workspace frontend build\" \"yarn dev:backend\"",
    "preview": "yarn workspace frontend preview",
    "check": "yarn workspace frontend check",
    "check:watch": "yarn workspace frontend check:watch",
    "build:app": "cross-env TILBOT_ENV=app yarn workspace frontend build",
    "app": "yarn build:app && yarn workspace app start",
    "package-win": "yarn build:app && yarn workspace app package-win",
    "package-linux": "yarn build:app && yarn workspace app package-linux",
    "package-mac-x64": "yarn build:app && yarn workspace app package-mac-x64",
    "package-mac-arm": "yarn build:app && yarn workspace app package-mac-arm",
    "test-unit": "npm run test --workspace=tests/unit",
    "test-integration": "npm run test --workspace=tests/integration",
    "test-integration-ui": "npm run test-ui --workspace=tests/integration",
    "test-integration-app": "yarn build:app && npm run test --workspace=tests/integration/app",
    "test-integration-app-ui": "yarn build:app && npm run test-ui --workspace=tests/integration/app"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^10.1.0",
    "tslib": "^2.3.1"
  },
  "packageManager": "yarn@4.13.0"
}
